import configColors from "./character/config/colors.js";
import configPartTypes from "./character/config/partTypes.js";
import configParts from "./character/config/parts.js";

const config = {
  colors: configColors,
  partTypes: configPartTypes,
  parts: configParts
};

export default config;
